# Справочная система по информационной безопасности

Веб-приложение для создания и ведения справочной системы по информационной безопасности. Система позволяет пользователям просматривать информацию о стандартах, лицензиях, определениях и методах защиты в сфере информационной безопасности. Администраторы могут редактировать, добавлять и удалять записи.

## 📋 Содержание

- [Технологии](#технологии)
- [Структура проекта](#структура-проекта)
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка и запуск](#установка-и-запуск)
- [Использование](#использование)
- [API Endpoints](#api-endpoints)
- [Безопасность](#безопасность)
- [Разработка](#разработка)

## 🛠 Технологии

- **Backend**: C++17 (HTTP сервер на сокетах)
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla JS)
- **База данных**: PostgreSQL 15
- **Контейнеризация**: Docker, Docker Compose
- **Библиотеки**:
  - libpq (PostgreSQL client library)
  - OpenSSL (для хеширования паролей)

## 📁 Структура проекта

```
curs-konst/
├── backend/
│   ├── main.cpp          # C++ HTTP сервер и API
│   └── Dockerfile        # Docker образ для бэкенда
├── frontend/
│   └── index.html        # Веб-интерфейс
├── database/
│   └── init.sql          # SQL скрипт инициализации БД
├── docker-compose.yml    # Конфигурация Docker Compose
├── .gitignore           # Игнорируемые файлы Git
└── README.md            # Документация
```

## ✨ Возможности

- 🔍 **Просмотр информации**: Пользователи могут просматривать информацию по различным разделам (Определения, Лицензии, Стандарты, Угрозы, Защита)
- 👤 **Регистрация и вход**: Пользователи могут создавать аккаунты и входить в систему
- 🔐 **Роли пользователей**: 
  - Обычные пользователи: только просмотр информации
  - Администраторы: полный доступ к управлению контентом
- ✏️ **Управление контентом** (только для администраторов):
  - Добавление новых записей
  - Редактирование существующих записей
  - Удаление записей
- 🛡️ **Безопасность**:
  - Защита от SQL инъекций (параметризованные запросы)
  - Хеширование паролей (SHA256)
  - Аутентификация через токены
  - CORS поддержка

## 📋 Требования

- Docker (версия 20.10 или выше)
- Docker Compose (версия 1.29 или выше)

Или для локальной разработки:

- C++ компилятор с поддержкой C++17 (g++ или clang++)
- PostgreSQL 15
- libpq-dev
- libssl-dev
- Nginx (для фронтенда)

## 🚀 Установка и запуск

### Быстрый запуск с Docker

1. Клонируйте репозиторий или скопируйте файлы проекта

2. Убедитесь, что порты 80, 8080 и 5432 свободны

3. Запустите проект с помощью Docker Compose:

```bash
docker-compose up --build
```

4. Откройте браузер и перейдите по адресу: `http://localhost`

### Остановка проекта

```bash
docker-compose down
```

Для удаления всех данных (включая базу данных):

```bash
docker-compose down -v
```

## 💻 Использование

### Вход как администратор

По умолчанию создан администраторский аккаунт:

- **Имя пользователя**: `admin`
- **Пароль**: `admin123`

⚠️ **Важно**: Измените пароль и логин админа ради боба

### Регистрация нового пользователя

1. Нажмите кнопку "Регистрация"
2. Заполните форму:
   - Имя пользователя
   - Email
   - Пароль
3. После успешной регистрации войдите в систему

### Просмотр информации

1. На главной странице отображаются все доступные разделы
2. Нажмите на карточку раздела, чтобы просмотреть его содержимое
3. Используйте кнопку "Назад к разделам" для возврата к списку разделов

### Управление контентом (только для администраторов)

После входа в систему как администратор:

1. **Добавление записи**:
   - Перейдите в нужный раздел
   - Нажмите кнопку "+ Добавить запись"
   - Заполните форму и сохраните

2. **Редактирование записи**:
   - В разделе нажмите кнопку "Редактировать" у нужной записи
   - Внесите изменения и сохраните

3. **Удаление записи**:
   - Нажмите кнопку "Удалить" у нужной записи
   - Подтвердите удаление

## 🔌 API Endpoints

### Публичные endpoints

#### `GET /api/sections`
Получить список всех разделов.

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "Определения",
    "description": "Основные термины и определения..."
  }
]
```

#### `GET /api/content?section_id=<id>`
Получить контент раздела. Если `section_id` не указан, возвращает весь контент.

**Ответ:**
```json
[
  {
    "id": 1,
    "section_id": 1,
    "title": "Информационная безопасность",
    "content_text": "Описание...",
    "created_at": "2024-01-01 00:00:00",
    "updated_at": "2024-01-01 00:00:00"
  }
]
```

#### `POST /api/register`
Регистрация нового пользователя.

**Тело запроса** (application/x-www-form-urlencoded):
```
username=user&email=user@example.com&password=pass123
```

**Ответ:**
```json
{
  "id": 2,
  "message": "User created"
}
```

#### `POST /api/login`
Вход в систему.

**Тело запроса** (application/x-www-form-urlencoded):
```
username=admin&password=admin123
```

**Ответ:**
```json
{
  "token": "abc123...",
  "user_id": 1,
  "role_id": 1
}
```

### Защищенные endpoints (требуют токен в заголовке Authorization)

#### `POST /api/content`
Создать новую запись (только администратор).

**Заголовки:**
```
Authorization: <token>
Content-Type: application/x-www-form-urlencoded
```

**Тело запроса:**
```
section_id=1&title=Название&content_text=Текст записи
```

#### `PUT /api/content/<id>`
Обновить запись (только администратор).

**Заголовки:**
```
Authorization: <token>
Content-Type: application/x-www-form-urlencoded
```

**Тело запроса:**
```
title=Новое название&content_text=Новый текст
```

#### `DELETE /api/content/<id>`
Удалить запись (только администратор).

**Заголовки:**
```
Authorization: <token>
```

## 🔒 Безопасность

### Защита от SQL инъекций

Все SQL-запросы используют параметризованные запросы через `PQexecParams`, что полностью исключает возможность SQL инъекций. Пример:

```cpp
const char* query = "SELECT * FROM users WHERE username = $1 AND password_hash = $2";
const char* paramValues[2] = {username, passwordHash};
PQexecParams(conn, query, 2, nullptr, paramValues, ...);
```

### Хеширование паролей

Пароли хешируются с помощью SHA256 перед сохранением в базу данных. В продакшн-окружении рекомендуется использовать bcrypt или Argon2.

### Аутентификация

Система использует токен- based аутентификацию. Токен генерируется при входе и должен передаваться в заголовке `Authorization` для защищенных endpoints.

## 🔧 Разработка

### Локальная разработка без Docker

#### Backend

1. Установите зависимости:
```bash
sudo apt-get update
sudo apt-get install build-essential libpq-dev libssl-dev
```

2. Скомпилируйте:
```bash
cd backend
g++ -std=c++17 -o main main.cpp -lpq -pthread -lcrypto
```

3. Убедитесь, что PostgreSQL запущен и создана база данных (см. `database/init.sql`)

4. Запустите:
```bash
./main
```

#### Frontend

Просто откройте `frontend/index.html` в браузере или используйте простой HTTP сервер:

```bash
cd frontend
python3 -m http.server 8000
```

### Структура базы данных

#### Таблица `roles`
- `id` (SERIAL PRIMARY KEY)
- `name` (VARCHAR) - название роли (admin, user)

#### Таблица `users`
- `id` (SERIAL PRIMARY KEY)
- `username` (VARCHAR UNIQUE) - имя пользователя
- `email` (VARCHAR UNIQUE) - email
- `password_hash` (VARCHAR) - хеш пароля
- `role_id` (INTEGER) - ссылка на roles.id
- `created_at` (TIMESTAMP)

#### Таблица `sections`
- `id` (SERIAL PRIMARY KEY)
- `name` (VARCHAR) - название раздела
- `description` (TEXT) - описание раздела
- `created_at` (TIMESTAMP)

#### Таблица `content`
- `id` (SERIAL PRIMARY KEY)
- `section_id` (INTEGER) - ссылка на sections.id
- `title` (VARCHAR) - заголовок записи
- `content_text` (TEXT) - текст записи
- `created_at` (TIMESTAMP)
- `updated_at` (TIMESTAMP)
- `author_id` (INTEGER) - ссылка на users.id

## 📝 Примечания

- В текущей реализации сессии хранятся в памяти сервера и теряются при перезапуске. В продакшн-окружении рекомендуется использовать Redis или хранить сессии в базе данных.
- Пароли хешируются с помощью SHA256. Для продакшн-окружения рекомендуется использовать bcrypt или Argon2.
- CORS настроен для разрешения всех источников (`Access-Control-Allow-Origin: *`). В продакшн-окружении это следует ограничить.

## 📄 Лицензия

Этот проект создан в учебных целях.

